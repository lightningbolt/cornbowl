<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<ul class="players">
<% @cornbowlers.each do |cornbowler| %>
  <li>
    <button type="button" class="add" value="<%= cornbowler.id %>">+</button>
    <%= cornbowler.name %>
  </li>
<% end %>
</ul>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= stylesheet_link_tag "games", :media => "all" %>
<%= javascript_include_tag "games" %>